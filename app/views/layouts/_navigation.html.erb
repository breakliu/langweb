<%= link_to "语言文字规范", root_path, class: "brand" %>
<ul class="nav">
  <li><%= link_to "首 页", root_path %></li>
  <% Type.all.each do |t| %>
  <li><%= link_to t.name, type_path(t) %></li>
  <% end %>
</ul>

<ul class="pull-right nav">
  <% if user_signed_in? %>
    <li><%= link_to '管 理', posts_path %></li>
    <li><%= link_to '注 销', destroy_user_session_path, :method=>'delete' %></li>
  <% else %>
    <li><%= link_to '登 录', new_user_session_path %></li>
  <% end %>
</ul>
